rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // The browser must use the authenticated FastAPI backend for all data access.
    // Cloud Run uses the Admin SDK, which is authorized through IAM instead.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
